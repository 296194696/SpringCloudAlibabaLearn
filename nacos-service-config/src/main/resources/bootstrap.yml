spring:
  application:
    name: service-config
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server‐addr: 47.115.225.148:8848
      config:
        server‐addr: 47.115.225.148:8848
        file-extension: yaml

# ${spring.application.name}-${spring.profile.active}.${spring.cloud.nacos.config.file-extension}
# service-config-dev.yaml

